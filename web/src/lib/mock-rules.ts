import type { ConfiguratorRule } from "@/types/configurator-rule";

/**
 * Placeholder rules removed — real rules come from the config DB via the API.
 * Empty fallback so imports keep working.
 */
export const MOCK_RULES: ConfiguratorRule[] = [];
