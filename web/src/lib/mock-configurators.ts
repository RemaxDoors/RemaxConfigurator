import type { Configurator } from "@/types/configurator";

/**
 * Placeholder configurators removed — the real definitions come from the config
 * DB via the Python API (/api/config). Empty fallback so imports keep working.
 */
export const MOCK_CONFIGURATORS: Configurator[] = [];
